#!/usr/bin/env bash
set -euo pipefail

target="${1:-}"
[[ -n "$target" ]] || exit 1

if command -v xdg-open >/dev/null 2>&1; then
  exec xdg-open "$target"
elif command -v open >/dev/null 2>&1; then
  exec open "$target"
fi

printf 'No open command found.\n' >&2
exit 1
