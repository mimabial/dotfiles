# Display panel

The editor UI includes code covered by the MIT license in `LICENSE`.

The integration uses the live Quickshell theme, `hyprshell` helpers, and
Hyprland-session startup. The shell-level preview guard survives monitor
topology changes and owns the Keep/Revert safety dialog.
