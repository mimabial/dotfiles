if [ "${IN_UWSM_ENV_PRELOADER:-}" != true ]; then
  for _uwsm_env_file in "${XDG_CONFIG_HOME:-$HOME/.config}"/uwsm/env-hyprland.d/*.sh; do
    [ -r "$_uwsm_env_file" ] && . "$_uwsm_env_file"
  done
  unset _uwsm_env_file
fi
