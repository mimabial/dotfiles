#!/usr/bin/env bash
set -euo pipefail

lib_file="${HOME}/.local/lib/hypr/core/agent-notify.sh"

if [[ ! -r "$lib_file" ]]; then
  printf 'agent-notify: missing library: %s\n' "$lib_file" >&2
  exit 1
fi

# shellcheck source=/dev/null
source "$lib_file"
agent_notify_main "$@"
