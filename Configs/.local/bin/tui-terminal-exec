#!/bin/bash
# Wrapper for TUI applications
# Launch TUI apps with $TERMINAL_TUI.

# Resolve TERMINAL_TUI from the environment, defaulting to kitty.
TERMINAL_TUI="${TERMINAL_TUI:-kitty}"

# Execute the terminal with all arguments passed through
exec "${TERMINAL_TUI}" "$@"
