"""In-tree monitor profile backend for Hyprland."""

# The panel compares the CLI's reported version against the running daemon's to
# decide whether the daemon is stale, so both must read the same constant.
VERSION = "2.0.0"
